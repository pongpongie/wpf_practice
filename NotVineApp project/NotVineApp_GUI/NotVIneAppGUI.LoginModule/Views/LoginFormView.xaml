<UserControl x:Class="NotVineAppGUI.LoginModule.Views.LoginFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NotVineAppGUI.LoginModule.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="TextBlockStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="Black" />
            <Setter Property="TextAlignment"
                    Value="left" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="Margin"
                    Value="5,2" />
        </Style>

        <Style x:Key="LoginInputBoxStyle"
               TargetType="TextBox">
            <Setter Property="TextAlignment"
                    Value="Center" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="Margin"
                    Value="5,2" />
            <Setter Property="Background"
                    Value="Transparent" />

        </Style>

        <Style x:Key="LoginInputPasswordBoxStyle"
               TargetType="PasswordBox">
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="Margin"
                    Value="5,2" />
            <Setter Property="Background"
                    Value="Transparent" />

        </Style>
    </UserControl.Resources>

    <Grid Width="200"
          Height="200"
          Background="Aquamarine">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   FontSize="20"
                   Text="{Binding TextBoxText}" />
        <TextBlock Grid.Row="1"
                   Style="{StaticResource TextBlockStyle}"
                   Text="아이디" />

        <Grid Grid.Row="2"
              VerticalAlignment="Center">
            <TextBox x:Name="IdTextBox"
                     Style="{StaticResource LoginInputBoxStyle}"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <TextBlock Grid.Row="3"
                   Style="{StaticResource TextBlockStyle}"
                   Text="비밀번호" />

        <Grid Grid.Row="4"
              VerticalAlignment="Center">
            <PasswordBox x:Name="PasswordInput"
                         Style="{StaticResource LoginInputPasswordBoxStyle}" />
        </Grid>

        <Button Grid.Row="5"
                Width="100"
                Height="30"
                VerticalAlignment="Center"
                Command="{Binding ClickCommand}"
                Content="로그인" />
    </Grid>
</UserControl>